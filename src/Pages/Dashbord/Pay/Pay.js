import React from 'react';
import { Outlet } from 'react-router-dom';

const Pay = () => {
    return (
        <div>
            <h2>pay comming soon</h2>
            <Outlet />
        </div>
    );
};

export default Pay;